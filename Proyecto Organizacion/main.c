#include <stdio.h>

// Declaración externa de la función de pruebas
void run_all_tests();

int main() {
    printf("Iniciando simulador MIPS...\n");
    run_all_tests();
    
    return 0;
}